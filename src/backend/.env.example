# Docker Compose reads .env automatically. Copy this file to .env if needed.
DOCS_URL=http://host.docker.internal:8071
DRIVE_URL=http://host.docker.internal:8072
DINUM_API_TIMEOUT=10
# For a host-run Django process export DOCS_URL=http://localhost:8071
# and DRIVE_URL=http://localhost:8072. manage.py does not load .env itself.
